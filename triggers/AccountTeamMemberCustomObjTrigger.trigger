trigger AccountTeamMemberCustomObjTrigger on Account_Team__c (before delete, before insert, before update, 
                                                              after delete, after insert, after update) 
{
     
}